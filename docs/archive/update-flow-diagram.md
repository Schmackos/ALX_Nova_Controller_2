# OTA Update Flow Diagram

## Visual Flow of Enhanced OTA Update Process

```
┌─────────────────────────────────────────────────────────────────────┐
│                         USER INTERACTION                             │
└─────────────────────────────────────────────────────────────────────┘

   User clicks "Check for Updates"
              │
              ▼
   ┌──────────────────────────┐
   │  ESP32 fetches           │
   │  version.txt from GitHub │
   └──────────────────────────┘
              │
              ▼
   ┌──────────────────────────┐
   │  Compare versions        │
   │  Current: 1.1.1          │
   │  Latest:  1.2.0          │
   └──────────────────────────┘
              │
              ▼
   ┌──────────────────────────┐     NO      ┌─────────────────────┐
   │  Update Available?       │─────────────▶│ "Firmware is up to  │
   └──────────────────────────┘              │  date"              │
              │ YES                           └─────────────────────┘
              ▼
   ┌──────────────────────────┐
   │  Display:                │
   │  ✓ Latest version info   │
   │  ✓ Release notes button  │
   │  ✓ Update button         │
   └──────────────────────────┘
              │
         ┌────┴────┐
         │         │
         ▼         ▼
   ┌─────────┐  ┌──────────────────────┐
   │ View    │  │ Click "Update to     │
   │ Release │  │ Latest Version"      │
   │ Notes   │  └──────────────────────┘
   └─────────┘            │
         │                ▼
         │      ┌──────────────────────┐
         │      │ Confirm Update?      │
         │      └──────────────────────┘
         │                │
         │                ▼
         │      ┌──────────────────────┐
         │      │ START OTA UPDATE     │
         │      └──────────────────────┘
         │                │
         │                ▼
         │
         │   ┌─────────────────────────────────────────────────┐
         │   │         OTA UPDATE PROCESS                      │
         │   └─────────────────────────────────────────────────┘
         │
         │   Status: "Preparing for update..."
         │   Progress: 0%
         │   Serial: "=== Starting OTA Update ==="
         │                │
         │                ▼
         │   Status: "Connecting to server..."
         │   Progress: 0%
         │   Serial: "Downloading from: https://..."
         │                │
         │                ▼
         │   Status: "Downloading: 16 / 330 KB"
         │   Progress: 5%
         │   Serial: "📦 Firmware size: 337920 bytes"
         │   Serial: "📥 Downloading firmware to flash..."
         │   Serial: "📊 Progress: 5% (16 KB / 330 KB)"
         │   ┌──────────────────────────┐
         │   │ Progress Bar: [█░░░░░░░] │
         │   └──────────────────────────┘
         │                │
         │                ▼
         │   Status: "Downloading: 66 / 330 KB"
         │   Progress: 20%
         │   Serial: "📊 Progress: 20% (66 KB / 330 KB)"
         │   ┌──────────────────────────┐
         │   │ Progress Bar: [████░░░░] │
         │   └──────────────────────────┘
         │                │
         │                ▼
         │   Status: "Downloading: 165 / 330 KB"
         │   Progress: 50%
         │   Serial: "📊 Progress: 50% (165 KB / 330 KB)"
         │   ┌──────────────────────────┐
         │   │ Progress Bar: [██████░░] │
         │   └──────────────────────────┘
         │                │
         │                ▼
         │   Status: "Downloading: 297 / 330 KB"
         │   Progress: 90%
         │   Serial: "📊 Progress: 90% (297 KB / 330 KB)"
         │   ┌──────────────────────────┐
         │   │ Progress Bar: [████████░] │
         │   └──────────────────────────┘
         │                │
         │                ▼
         │   Status: "Downloading: 330 / 330 KB"
         │   Progress: 100%
         │   Serial: "📊 Progress: 100% (330 KB / 330 KB)"
         │   ┌──────────────────────────┐
         │   │ Progress Bar: [█████████] │
         │   └──────────────────────────┘
         │                │
         │                ▼
         │   Status: "Verifying firmware..."
         │   Progress: 100%
         │   Serial: "✅ Download complete. Verifying..."
         │                │
         │                ▼
         │   Status: "Update complete! Rebooting..."
         │   Progress: 100%
         │   Serial: "✅ OTA update completed successfully!"
         │   Serial: "🔄 Rebooting device in 3 seconds..."
         │                │
         │                ▼
         │      ┌──────────────────────┐
         │      │   ESP32 REBOOTS      │
         │      └──────────────────────┘
         │                │
         │                ▼
         │      ┌──────────────────────┐
         │      │ New Firmware Running │
         │      │ Version: 1.2.0       │
         │      └──────────────────────┘
         │
         │
         ▼
   ┌──────────────────────────────────────┐
   │  RELEASE NOTES MODAL                 │
   │  ┌────────────────────────────────┐  │
   │  │ Release Notes           [X]    │  │
   │  ├────────────────────────────────┤  │
   │  │                                │  │
   │  │ # Version 1.2.0 Release Notes │  │
   │  │                                │  │
   │  │ ## New Features                │  │
   │  │ ✨ Progress bar                │  │
   │  │ ✨ Release notes viewer        │  │
   │  │ ✨ Status messages             │  │
   │  │                                │  │
   │  │ ## Improvements                │  │
   │  │ 🔧 Enhanced logging            │  │
   │  │ 🔧 Better error handling       │  │
   │  │                                │  │
   │  │ ## Bug Fixes                   │  │
   │  │ 🐛 Fixed LED timing            │  │
   │  │ 🐛 WiFi stability              │  │
   │  │                                │  │
   │  └────────────────────────────────┘  │
   └──────────────────────────────────────┘
              │
              ▼
   User clicks [X] or outside
              │
              ▼
   Modal closes, returns to main page
```

## Error Handling Flow

```
   During any step of update process
              │
              ▼
   ┌──────────────────────────┐
   │  Error Occurs?           │
   └──────────────────────────┘
              │ YES
              ▼
   ┌──────────────────────────────────────┐
   │  Determine Error Type:               │
   │  • HTTP Error (404, 500, etc)        │
   │  • Invalid firmware size             │
   │  • Not enough storage                │
   │  • Flash write error                 │
   │  • Verification failed               │
   └──────────────────────────────────────┘
              │
              ▼
   ┌──────────────────────────────────────┐
   │  Display Error:                      │
   │  • Web UI: Red error message         │
   │  • Serial: ❌ Detailed error         │
   │  • Hide progress bar                 │
   │  • Re-enable update button           │
   └──────────────────────────────────────┘
              │
              ▼
   ┌──────────────────────────────────────┐
   │  User can:                           │
   │  • Try again                         │
   │  • Check Serial for details          │
   │  • Fix issue (WiFi, space, etc)      │
   └──────────────────────────────────────┘
```

## WebSocket Communication Flow

```
   ESP32                        Web Browser
     │                               │
     │   ─────────────────────────▶  │  Initial connection
     │   WebSocket connected         │
     │                               │
     │   ◀─────────────────────────  │  Request initial status
     │                               │
     │   ─────────────────────────▶  │  Send LED state
     │   ─────────────────────────▶  │  Send blinking state
     │   ─────────────────────────▶  │  Send WiFi status
     │                               │
     │                               │
     │   ◀─────────────────────────  │  User clicks "Update"
     │                               │
     │   ─────────────────────────▶  │  status: "preparing"
     │                               │  progress: 0%
     │                               │
     │   ─────────────────────────▶  │  status: "downloading"
     │                               │  progress: 5%
     │                               │  bytes: 16KB / 330KB
     │                               │
     │   ─────────────────────────▶  │  status: "downloading"
     │                               │  progress: 10%
     │                               │  bytes: 33KB / 330KB
     │                               │
     │   ... (every 5% or 2 seconds) │
     │                               │
     │   ─────────────────────────▶  │  status: "downloading"
     │                               │  progress: 100%
     │                               │  bytes: 330KB / 330KB
     │                               │
     │   ─────────────────────────▶  │  status: "complete"
     │                               │  progress: 100%
     │                               │  message: "Rebooting..."
     │                               │
     │  [REBOOT]                     │
     │                               │
     │   ─────────────────────────▶  │  WebSocket reconnects
     │   New version running         │
     │                               │
```

## Data Flow Diagram

```
┌──────────────┐
│   GitHub     │
│  Repository  │
└──────┬───────┘
       │
       │ ESP32 fetches via HTTPClient
       │
       ├─────────────────┬──────────────────┐
       │                 │                  │
       ▼                 ▼                  ▼
┌─────────────┐  ┌──────────────┐  ┌──────────────┐
│ version.txt │  │ firmware.bin │  │ releases/    │
│   "1.2.0"   │  │  (binary)    │  │  1.2.0.md    │
└─────────────┘  └──────────────┘  └──────────────┘
       │                 │                  │
       │                 │                  │
       ▼                 ▼                  ▼
┌───────────────────────────────────────────────────┐
│                  ESP32 Device                     │
│  ┌────────────────────────────────────────────┐  │
│  │  handleCheckUpdate()                       │  │
│  │  • Compares versions                       │  │
│  │  • Sets updateAvailable flag               │  │
│  └────────────────────────────────────────────┘  │
│                                                   │
│  ┌────────────────────────────────────────────┐  │
│  │  handleGetReleaseNotes()                   │  │
│  │  • Fetches markdown from GitHub            │  │
│  │  • Returns formatted JSON                  │  │
│  └────────────────────────────────────────────┘  │
│                                                   │
│  ┌────────────────────────────────────────────┐  │
│  │  performOTAUpdate()                        │  │
│  │  • Downloads firmware                      │  │
│  │  • Updates progress variables              │  │
│  │  • Broadcasts status via WebSocket         │  │
│  │  • Writes to flash memory                  │  │
│  │  • Verifies and reboots                    │  │
│  └────────────────────────────────────────────┘  │
│                                                   │
│  ┌────────────────────────────────────────────┐  │
│  │  broadcastUpdateStatus()                   │  │
│  │  • Sends JSON via WebSocket                │  │
│  │  • All connected clients receive update    │  │
│  └────────────────────────────────────────────┘  │
└───────────────────────────────────────────────────┘
       │
       │ WebSocket (ws://ip:81)
       │ REST API (http://ip:80/api/*)
       │
       ▼
┌───────────────────────────────────────────────────┐
│              Web Browser (Client)                 │
│  ┌────────────────────────────────────────────┐  │
│  │  JavaScript                                │  │
│  │  • Receives WebSocket messages             │  │
│  │  • Updates progress bar                    │  │
│  │  • Updates status text                     │  │
│  │  • Fetches release notes via API           │  │
│  │  • Displays modal popup                    │  │
│  └────────────────────────────────────────────┘  │
│                                                   │
│  ┌────────────────────────────────────────────┐  │
│  │  HTML/CSS                                  │  │
│  │  • Progress bar visual                     │  │
│  │  • Status messages                         │  │
│  │  • Release notes modal                     │  │
│  │  • Buttons and controls                    │  │
│  └────────────────────────────────────────────┘  │
└───────────────────────────────────────────────────┘
```

## State Machine Diagram

```
┌─────────┐
│  IDLE   │
└────┬────┘
     │ User clicks "Check for Updates"
     ▼
┌─────────────┐
│  CHECKING   │
└────┬────────┘
     │
     ├───▶ No update available ───▶ Return to IDLE
     │
     │ Update available
     ▼
┌──────────────────┐
│ UPDATE_AVAILABLE │
└────┬─────────────┘
     │
     ├───▶ User clicks "View Release Notes" ───▶ SHOWING_NOTES ───▶ Return
     │
     │ User clicks "Update"
     ▼
┌─────────────┐
│  PREPARING  │  ← otaStatus = "preparing"
└────┬────────┘    otaProgress = 0%
     │
     ▼
┌──────────────┐
│  CONNECTING  │  ← otaStatusMessage = "Connecting to server..."
└────┬─────────┘
     │
     ▼
┌──────────────┐
│ DOWNLOADING  │  ← otaStatus = "downloading"
└────┬─────────┘    otaProgress = 0-100%
     │              Broadcasts every 5% or 2 seconds
     │
     ▼
┌──────────────┐
│  VERIFYING   │  ← otaStatusMessage = "Verifying firmware..."
└────┬─────────┘    otaProgress = 100%
     │
     ├───▶ Verification failed ───▶ ERROR ───▶ Return to UPDATE_AVAILABLE
     │
     │ Success
     ▼
┌──────────────┐
│   COMPLETE   │  ← otaStatus = "complete"
└────┬─────────┘    otaStatusMessage = "Update complete! Rebooting..."
     │
     ▼
┌──────────────┐
│  REBOOTING   │
└────┬─────────┘
     │
     ▼
┌──────────────┐
│   RUNNING    │  ← New firmware version
└──────────────┘
```

---

This diagram shows the complete flow of the enhanced OTA update system,
from checking for updates to successful installation with visual progress
and release notes.
